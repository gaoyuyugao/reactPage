// services/api.js
export async function login(username, password) {
    // 这里是接口占位符，将来可以在此实现实际的 API 调用
    console.log('Logging in with:', username, password);
    // 假设登录成功，返回 true。未来可以改为 API 返回结果。
    return true;
  }
  